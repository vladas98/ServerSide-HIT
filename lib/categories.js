'use strict';

/* The categories a cost item can belong to. "sport" is intentionally
   singular, and the order matches the sample report in the project
   specification, since the report lists its categories in this order. */
const CATEGORIES = ['food', 'education', 'health', 'housing', 'sport'];

module.exports = { CATEGORIES };
