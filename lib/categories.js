'use strict';

// The categories a cost item can belong to. "sport" is intentionally
// singular to match the exact key used in the required report JSON shape.
const CATEGORIES = ['food', 'health', 'housing', 'sport', 'education'];

module.exports = { CATEGORIES };
